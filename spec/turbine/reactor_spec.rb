describe Turbine::Reactor do
  describe ".current" do
    it "returns the current reactor if inside a reactor"
    it "returns nil if not inside a reactor"
  end

  describe "#crashed?" do

  end

  describe "#spawn" do
  end
end
